######################################################################
# 1. ОБЩИЕ СИСТЕМНЫЕ ФАЙЛЫ (General / OS)
######################################################################
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs (Общие логи)
*.log

######################################################################
# 2. СРЕДЫ РАЗРАБОТКИ (IDE & Editors)
######################################################################
# Visual Studio & MSBuild
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# JetBrains (Rider, WebStorm)
# Важно: Мы игнорируем workspace.xml (личные настройки окон),
# но иногда полезно оставить файлы запуска (run configurations), если они шарятся.
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/usage.statistics.xml
.idea/**/dictionaries
.idea/**/shelf

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

######################################################################
# 3. .NET / C# (Backend)
######################################################################
# Build results
[Bb]in/
[Oo]bj/
build/

# NuGet
*.nupkg
*.snupkg
# The packages folder can be ignored because of Package Restore
**/[Pp]ackages/*
# except build/, which is used as an MSBuild target.
!**/[Pp]ackages/build/

# .NET Core & ASP.NET Core specific
project.lock.json
project.fragment.lock.json
artifacts/

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# Unit Testing & Coverage
TestResults/
*.trdx
*.visualstudio.testsettings.testrunconfig
coverage/
coverage.json
coverage.xml
*_coverage/

# Application Insights & Perf
component.json
profiler/

######################################################################
# 4. NODE.JS & REACT (Frontend)
######################################################################
# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production Build Artifacts (React / Vite / Next.js)
build/
dist/
.next/
out/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment Variables (CRITICAL)
# В React проектах часто используют .env.local для локальных оверрайдов
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Исключение: .env.example (шаблон) стоит оставить
!.env.example

# Cache mechanisms
.eslintcache
.stylelintcache
*.tsbuildinfo

# Parcel-bundler cache
.cache
.parcel-cache

######################################################################
# 5. SECURITY & CONFIGURATION
######################################################################
# Secrets for .NET (User Secrets id is usually in csproj, actual secrets outside)
# Но если вдруг кто-то создал json локально:
secrets.json
appsettings.Local.json
appsettings.Development.local.json

# Игнорируем PEM ключи и сертификаты (если они случайно попали в папку)
*.pem
*.key
*.cer
*.pfx
*.p12

######################################################################
# 6. MISC
######################################################################
# Backup files generated by various editors
*~
*.bak
*.swp